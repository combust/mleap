package ml.combust.mleap.databricks.runtime.testkit

import org.apache.spark.sql.SparkSession

class TestXgboost(session: SparkSession) extends Runnable {
  override def run(): Unit = {
    println("Still need to test")
  }
}
