[tox]
envlist = py36,py37
skipdist = true

[testenv]
deps =
        -rrequirements-dev-spark3.0.txt
        nose
        coverage
commands =
        nosetests --with-coverage \
                --cover-package=mleap \
                --nologcapture \
                --exclude-dir=mleap/pyspark \
                --verbose \
                --where ./ \
                {posargs}

[testenv:venv]
envdir = venv
commands =
